
services:
  dev-db:
    image: postgres
    container_name: dev-db
    ports:
      - "5434:5432"
    environment:
       POSTGRES_USER: postgres
       POSTGRES_PASSWORD: 123
       POSTGRES_DB: nest
    networks:
      - freecodecamp

networks:
 freecodecamp:
   
